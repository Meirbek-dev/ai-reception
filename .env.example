# Development environment variables (used by local docker/dev scripts)
# Keep secrets out of repo in production; this file is safe for local defaults.
ENVIRONMENT=development
UVICORN_HOST=0.0.0.0
UVICORN_PORT=5040
UVICORN_WORKERS=1
TESSDATA_PREFIX=/usr/share/tessdata/
# Optional: increase logging verbosity
LOG_LEVEL=debug
# Where to persist uploads (host path mounted into container)
UPLOADS_DIR=./api/uploads
APP_DATABASE_URL=sqlite+aiosqlite:///./data/ai_reception.db
# Session secret - MUST be changed in production to a long random string
APP_SESSION_SECRET_KEY=dev-secret-change-in-production-use-openssl-rand-hex-32
